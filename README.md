# WebSocket Real-Time Chat Project

Багатокористувацький чат-додаток, побудований на сучасних технологіях: **Angular 21** для клієнта та **NestJS 11** для бекенда. Додаток підтримує обмін повідомленнями в реальному часі через WebSockets та зберігає історію в базі даних **PostgreSQL**.

## 🚀 Стек технологій

- **Frontend**: Angular 21+, Signal-based logic, SCSS, BEM, Standalone components.
- **Backend**: NestJS 11, Socket.io, TypeORM.
- **Database**: PostgreSQL 15.
- **DevOps**: Docker, Docker Compose.

---

## 🛠 Як запустити проект

Найшвидший спосіб розгорнути весь проект — використовувати **Docker Compose**.

### Запуск через Docker

1. Переконайтеся, що ваш Docker Desktop запущено.
2. Виконайте команду в корені проекту:
   ```powershell
   docker-compose up --build -d
   ```
3. Після завершення:
   - Клієнт: [http://localhost:4200](http://localhost:4200)
   - Сервер (API/WS): [http://localhost:3000](http://localhost:3000)
   - База даних: `localhost:5432`

### Доступ у локальній мережі (Wi-Fi)
Додаток налаштовано для роботи за адресою:
**[http://192.168.0.198:4200](http://192.168.0.198:4200)**

---

## 🧹 Як зупинити та почистити проект

Для зупинки контейнерів:
```powershell
docker-compose stop
```

Для повного видалення контейнерів, мереж та образів проекту:
```powershell
docker-compose down
```

Якщо ви хочете також видалити дані бази даних (volumes):
```powershell
docker-compose down -v
```

---

## 🧪 Тестування

### Клієнт (Angular)
Перейдіть у папку `client` та запустіть тести (virest):
```powershell
cd client
npm test
```

### Сервер (NestJS)
Перейдіть у папку `server` та запустіть модульні або e2e тести (jest):
```powershell
cd server
npm test        # Unit tests
npm run test:e2e # End-to-end tests
```

---

## 📂 Структура проекту

- `/client` - Angular додаток.
- `/server` - NestJS сервер.
- `docker-compose.yml` - Конфігурація для розгортання всього стеку.
- `.gitignore` - Список ігнорованих файлів для Git.
