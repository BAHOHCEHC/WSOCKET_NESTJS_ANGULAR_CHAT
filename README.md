# WebSocket Real-Time Chat Project

Багатокористувацький чат-додаток, побудований на сучасних технологіях: **Angular 21** для клієнта та **NestJS 11** для бекенда. Додаток підтримує обмін повідомленнями в реальному часі через WebSockets та зберігає історію в базі даних **PostgreSQL**.

---

## 🛠 Як запустити проект

Найшвидший спосіб розгорнути весь проект — використовувати **Docker Compose**.

### Автоматичний запуск з очищенням (Windows)
Я додав скрипт `start-project.ps1`, який автоматично видаляє старі дані (volumes), запускає проект та відкриває браузер:
1. Кликніть правою кнопкою на `start-project.ps1` -> **Run with PowerShell**.
2. Або виконайте в терміналі:
   ```powershell
   .\start-project.ps1
   ```

### Ручний запуск через Docker
Виконайте команду в корені проекту:
```powershell
docker-compose up --build -d
```
- Клієнт: [http://localhost:4200](http://localhost:4200) (відкриється автоматично, якщо запускати через `npm start` локально).

---

## 🧹 Як зупинити та почистити проект

Для зупинки контейнерів:
```powershell
docker-compose stop
```

Для повного видалення контейнерів, мереж та **очищення всіх даних (volumes)**:
```powershell
docker-compose down -v
```

---

## 🚀 Стек технологій
- **Frontend**: Angular 21+ (запуск з `-o` для авто-відкриття).
- **Backend**: NestJS 11.
- **Database**: PostgreSQL 15.

---

## 🧪 Тестування

### Клієнт (Angular)
```powershell
cd client
npm test
```

### Сервер (NestJS)
```powershell
cd server
npm test
npm run test:e2e
```
